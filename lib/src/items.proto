syntax = "proto3";

package format;

message Client {
  string CurrentPlayer = 1;
}

message Server {
  oneof Command {
    string SetPlayer = 1;
    bool GetPlayer = 2;
    bool PlayerStopped = 3;
  };
}
