syntax = "proto3";

package format;

message Client {
  string GetFocusedPlayer = 1;
}

message Server {
  oneof Command {
    string SetFocusedPlayer = 1;
    bool GetPlayer = 2;
    bool UnfocusPlayer = 3;
  };
}
