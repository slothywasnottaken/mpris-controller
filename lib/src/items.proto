syntax = "proto3";

package format;

message Client {
  oneof Message {
  string FocusedPlayer = 1;
  bool CouldNotFindPlayer = 2;
  }
}

message Server {
  oneof Command {
    string SetFocusedPlayer = 1;
    bool GetPlayer = 2;
    bool UnfocusPlayer = 3;
  };
}
